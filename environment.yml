name: _env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - numpy<2
  - pandas
  - tqdm
  - pillow
  - nltk
  - pip:
      - paddleocr==2.6.1.3
      - deep-translator
      - transformers
      - evaluate
      - torch
      - torchvision
      - torchaudio
      - openai
      - langchain==0.1.20
      - httpx==0.24.1
      - idna==2.10
      - async-timeout<5.0.0
      - packaging<25
